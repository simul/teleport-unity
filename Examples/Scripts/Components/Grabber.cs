using UnityEngine;
using uid = System.UInt64;

namespace teleport
{
	/// <summary>
	/// An example component to allow objects to be grabbed. This component is attached to the grabbing object,
	/// corresponding to a user's hand for example.
	/// </summary>
	public class Grabber : MonoBehaviour
	{
		// Start is called before the first frame update
		void Start()
		{
		}

		// Update is called once per frame
		void Update()
		{
		
		}
	}
}